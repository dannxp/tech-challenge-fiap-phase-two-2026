spring.application.name=restaurante
server.port=8080

# PostgreSQL
spring.datasource.url=jdbc:postgresql://localhost:5432/restaurante
spring.datasource.username=restaurante
spring.datasource.password=restaurante123
spring.datasource.driver-class-name=org.postgresql.Driver

# SQL initialization
spring.sql.init.mode=always
spring.sql.init.schema-locations=classpath:schema.sql

# Logging
logging.level.com.example.sistema.restaurante=DEBUG
